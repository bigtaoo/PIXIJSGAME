/node_modules
wechatgame/project.config.json

# Web build outputs
/dist
/crazygames

# Capacitor native projects — generated by CI (cap add ios/android).
# Never commit; GitHub Actions regenerates them on every run.
/ios/
/android/


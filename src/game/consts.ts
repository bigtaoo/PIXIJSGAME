export const GAME_WIDTH = 1080;
export const GAME_HEIGHT = 1920;
export const OFFSET_Y = 300;

// Header bar dimensions (must stay in sync with landscapeLayout / portraitLayout in header.ts).
// The grid is constrained to these bounds so its left/right edges align with the header bar.
export const HEADER_X_PORTRAIT   = 30;
export const HEADER_BAR_W_PORTRAIT  = 1020;
export const HEADER_X_LANDSCAPE  = 350;
export const HEADER_BAR_W_LANDSCAPE = 1350;
